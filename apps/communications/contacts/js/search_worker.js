'use strict';

(function(wutils) {
  var canceled = false,
      contactsToSearch;

  wutils.addEventListener('message',processMessage);

  function processMessage(e) {
    var message = e.data;
  }

  function performSearch() {
    
  }
})(self);
